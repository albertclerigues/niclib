Evaluation metrics
====================================

The metrics provided in niclib are compatible with both numpy arrays and torch tensors as inputs. The returned
value will be a python scalar in the case of a numpy entry or a torch tensor if the input was a torch tensor.

.. automodule:: niclib.metrics
	:members: